I observed both order a and order b has contain same values, so I done merging operation and created df3 dataframe .
Business rules and tasks are completed and they are in ex.ipynb file
sql queries are in sql_query.sql file
